# Development tooling
.cursor/
.git/
.github/
node_modules/

# Supabase project files (Deno Edge Functions, SQL migrations — not part of Vercel build)
supabase/

# Build artifacts
dist/
build/
coverage/
.nyc_output/

# Documentation (not needed at runtime)
docs/
*.md
!README.md

# Environment files (never deploy secrets; example is fine)
.env*
!.env.example

# Test and lint config
vitest.config.*
.eslintrc.*
# NOTE: tsconfig.node.json must NOT be ignored — it's referenced by tsconfig.json
# via `references: [{ path: ./tsconfig.node.json }]` and removing it breaks tsc

# Logs and OS noise
*.log
.DS_Store
Thumbs.db
