node_modules
dist
# Ignore every dotenv variant (.env.production, .env.development,
# .env.local.backup, ...) — the previous two-line list only covered .env and
# .env.local, so any other variant would have been committed.
.env*
!.env.example
.DS_Store
*.log
coverage
.vscode
.idea

# Supabase CLI local artifacts
supabase/.temp
supabase/.branches

# Local staging for the DMP CSV exports. These hold real deceased and
# next-of-kin names and this repo is public — never commit them.
# See docs/15-data-import.md.
scripts/import/data/
scripts/import/sql/

# Agent worktrees (transient)
.claude/worktrees/
