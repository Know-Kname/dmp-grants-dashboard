/**
 * Data hooks for every DMP CMS module.
 *
 * The implementations live in `./data`, one file per domain. This file stays
 * as the public entry point so existing imports keep working; prefer importing
 * from here rather than reaching into `./data/*` directly.
 */
export * from './data';
